package com.erwo.greendao.gen;

import java.util.List;
import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;
import org.greenrobot.greendao.query.Query;
import org.greenrobot.greendao.query.QueryBuilder;

import com.example.dbgreen.bean.Son;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "SON".
*/
public class SonDao extends AbstractDao<Son, Long> {

    public static final String TABLENAME = "SON";

    /**
     * Properties of entity Son.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property SonName = new Property(1, String.class, "sonName", false, "SON_NAME");
        public final static Property SonHome = new Property(2, String.class, "sonHome", false, "SON_HOME");
        public final static Property SonTag = new Property(3, int.class, "sonTag", false, "SON_TAG");
        public final static Property FatherId = new Property(4, long.class, "fatherId", false, "FATHER_ID");
    }

    private Query<Son> father_SonListQuery;

    public SonDao(DaoConfig config) {
        super(config);
    }
    
    public SonDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"SON\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"SON_NAME\" TEXT," + // 1: sonName
                "\"SON_HOME\" TEXT," + // 2: sonHome
                "\"SON_TAG\" INTEGER NOT NULL ," + // 3: sonTag
                "\"FATHER_ID\" INTEGER NOT NULL );"); // 4: fatherId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"SON\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Son entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String sonName = entity.getSonName();
        if (sonName != null) {
            stmt.bindString(2, sonName);
        }
 
        String sonHome = entity.getSonHome();
        if (sonHome != null) {
            stmt.bindString(3, sonHome);
        }
        stmt.bindLong(4, entity.getSonTag());
        stmt.bindLong(5, entity.getFatherId());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Son entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String sonName = entity.getSonName();
        if (sonName != null) {
            stmt.bindString(2, sonName);
        }
 
        String sonHome = entity.getSonHome();
        if (sonHome != null) {
            stmt.bindString(3, sonHome);
        }
        stmt.bindLong(4, entity.getSonTag());
        stmt.bindLong(5, entity.getFatherId());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public Son readEntity(Cursor cursor, int offset) {
        Son entity = new Son( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // sonName
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // sonHome
            cursor.getInt(offset + 3), // sonTag
            cursor.getLong(offset + 4) // fatherId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Son entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setSonName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setSonHome(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setSonTag(cursor.getInt(offset + 3));
        entity.setFatherId(cursor.getLong(offset + 4));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(Son entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(Son entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(Son entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "sonList" to-many relationship of Father. */
    public List<Son> _queryFather_SonList(long fatherId) {
        synchronized (this) {
            if (father_SonListQuery == null) {
                QueryBuilder<Son> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.FatherId.eq(null));
                father_SonListQuery = queryBuilder.build();
            }
        }
        Query<Son> query = father_SonListQuery.forCurrentThread();
        query.setParameter(0, fatherId);
        return query.list();
    }

}

package com.erwo.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.example.dbgreen.bean.CustomerWithOrder;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "CUSTOMER_WITH_ORDER".
*/
public class CustomerWithOrderDao extends AbstractDao<CustomerWithOrder, Long> {

    public static final String TABLENAME = "CUSTOMER_WITH_ORDER";

    /**
     * Properties of entity CustomerWithOrder.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property CusId = new Property(1, Long.class, "cusId", false, "CUS_ID");
        public final static Property OderId = new Property(2, Long.class, "oderId", false, "ODER_ID");
    }


    public CustomerWithOrderDao(DaoConfig config) {
        super(config);
    }
    
    public CustomerWithOrderDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"CUSTOMER_WITH_ORDER\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"CUS_ID\" INTEGER," + // 1: cusId
                "\"ODER_ID\" INTEGER);"); // 2: oderId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"CUSTOMER_WITH_ORDER\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, CustomerWithOrder entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Long cusId = entity.getCusId();
        if (cusId != null) {
            stmt.bindLong(2, cusId);
        }
 
        Long oderId = entity.getOderId();
        if (oderId != null) {
            stmt.bindLong(3, oderId);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, CustomerWithOrder entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Long cusId = entity.getCusId();
        if (cusId != null) {
            stmt.bindLong(2, cusId);
        }
 
        Long oderId = entity.getOderId();
        if (oderId != null) {
            stmt.bindLong(3, oderId);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public CustomerWithOrder readEntity(Cursor cursor, int offset) {
        CustomerWithOrder entity = new CustomerWithOrder( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1), // cusId
            cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2) // oderId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, CustomerWithOrder entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setCusId(cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1));
        entity.setOderId(cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(CustomerWithOrder entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(CustomerWithOrder entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(CustomerWithOrder entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
